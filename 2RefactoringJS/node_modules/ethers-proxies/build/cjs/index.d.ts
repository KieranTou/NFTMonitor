import { BlockTag, Provider } from '@ethersproject/abstract-provider';
declare const detectProxyTarget: (proxyAddress: string, provider: Provider, blockTag?: BlockTag | Promise<BlockTag> | undefined) => Promise<string | null>;
export default detectProxyTarget;
